var salmonSteve = {
	minCst: 1,
	maxCst: 10,
	pricePer: 1,
	this.getHour {
		return cookieCount(minCst, maxCst, pricePer);
	}

}

function cookieCount(minCst, maxCst, pricePer) {


}
//Taken from MDN
function randInt(min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}
